# Architecture Decision Records (ADR)

Add a new file `NNNN-title.md` when a structural decision:

- is costly to reverse,
- affects multiple features,
- or diverges from defaults in `docs/STRUCTURE.md`.

Lightweight template: see `0001-record-architecture-decisions.md`.
